model {
	for (i in 1:N) {
		lambdaC12[i] <- exp(betaC12 + uC[id[i]])
		lambdaC13[i] <- exp(betaC12 + uC[id[i]])
		lambdaC23[i] <- exp(betaC12 + uC[id[i]])
		T[i] ~ dinterval(t[i], lim[i,])
		t[i] ~ dweib(rho, lambda[i])
	}
	sigma ~ dunif(0, 100)
	tau <- 1/sigma
	beta0 ~ dnorm(0.0, 1.0E-4)
	beta1 ~ dnorm(0.0, 1.0E-4)
	beta2 ~ dnorm(0.0, 1.0E-4)
	rho ~ dgamma(1.0E-3, 1.0E-3)
}